const form=document.querySelector('#loginForm'),error=document.querySelector('#loginError'),submit=form.querySelector('.submit');
fetch('/api/auth/status').then(r=>r.json()).then(status=>{if(status.authenticated)window.location.href='/';if(!status.configured)document.querySelector('#setupWarning').hidden=false;}).catch(()=>error.textContent='Unable to reach Ledgerly. Please try again.');
document.querySelector('#togglePassword').addEventListener('click',e=>{const input=document.querySelector('#password'),show=input.type==='password';input.type=show?'text':'password';e.currentTarget.textContent=show?'Hide':'Show';e.currentTarget.setAttribute('aria-label',show?'Hide password':'Show password');});
form.addEventListener('submit',async e=>{e.preventDefault();error.textContent='';submit.disabled=true;submit.firstChild.textContent='Signing in ';try{const response=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({username:form.username.value,password:form.password.value})}),data=await response.json();if(!response.ok)throw new Error(data.error||'Sign in failed');window.location.href='/';}catch(err){error.textContent=err.message;submit.disabled=false;submit.firstChild.textContent='Sign in ';}});
